apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ai-native-api
  namespace: ai-native
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ai-native-api
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 65
