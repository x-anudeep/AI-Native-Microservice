apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-native.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "ai-native.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
